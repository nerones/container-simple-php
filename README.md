# container-simple-php
Docker container build to develop with php and apache as the web server.

Builded from the oficial docker php repository https://registry.hub.docker.com/_/php/ with some little tweaks.
